// let count = 0;

module.exports = (node, ctx, processor) => {
  // console.log('*** rulset ***', node.content.length);
  // console.log(ctx.uri);
  // console.log(JSON.stringify(node, null, 2));
  // count = count + 1;

  // if (count === 3) {
  //   process.exit(-1);
  // }

  return ctx;
};
